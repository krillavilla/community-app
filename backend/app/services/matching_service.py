"""Rule-based matching service placeholder.
Implement simple deterministic rules in Phase 2; replace/augment with ML service in Phase 3.
"""
from typing import List, Dict


def suggest_matches_for_user(user_id: int) -> List[Dict]:
    """Return a list of suggested user matches (placeholder).
    Structure: [{"user_id": 2, "score": 0.5, "rationale": "shared habits"}, ...]
    """
    return []
